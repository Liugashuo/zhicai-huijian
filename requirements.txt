# 运行与离线测试所需（核心算法仅依赖标准库 + 以下解析/报告库）
pdfplumber>=0.11
pypdf>=4.0
reportlab>=4.0
streamlit>=1.35

# 可选：接入真实 Qwen3（Ollama / vLLM 的 OpenAI 兼容接口）
# openai>=1.0

# 可选：浏览器自动化（browser-use 走 CDP，真实环境启用）
# browser-use
